extends DiscordClient

## Convenience autoload. Registered as the `Discord` singleton by the plugin.
## DiscordClient is a Node provided by the GDExtension; this script just lets
## the plugin expose one shared instance. All methods/signals come from the
## native DiscordClient class — see the addon README.
